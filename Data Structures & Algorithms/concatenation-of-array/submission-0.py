class Solution:
    def getConcatenation(self, nums: List[int]) -> List[int]:
        newArr = [] 
        for n in nums:
            newArr.append(n)
        for n in nums:
            newArr.append(n)
        return newArr